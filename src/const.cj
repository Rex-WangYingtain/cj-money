package cj_money

import std.math.numeric.*

const DefaultDBMoneyValueSeparator: String = "|"
let BigInt_ZERO: BigInt = BigInt(0)
let BigInt_ONE: BigInt = BigInt(1)
let BigInt_TWO: BigInt = BigInt(2)
let BigInt_MINUS_ONE: BigInt = BigInt(-1)
let BigInt_TEN: BigInt = BigInt(10)
let BigInt_FIVE: BigInt = BigInt(5)


// cjpm build --coverage
// cjpm test --coverage
// cjcov -o output --html-details